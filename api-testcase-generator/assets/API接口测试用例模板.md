# 接口 API 测试用例模板

> 适用范围：基于接口文档/OpenAPI/接口表进行接口测试用例设计与执行记录  
> 覆盖维度建议：请求与参数、响应与错误码、鉴权与权限、幂等与并发（按需）  
> 输出目标：可执行、可复现、可追溯、可用于回归与自动化候选筛选

---

## 1. 用例元信息

- **用例编号**：API-<模块>-<三位序号>（例：API-AUTH-001）
- **用例标题**：在【输入/条件】下调用【接口】，预期【结果】
- **所属模块/子模块**：
- **需求/用户故事/缺陷关联**：PRD-xxx / US-xxx / BUG-xxx
- **接口信息**：
  - **接口名称**：
  - **Method**：GET / POST / PUT / DELETE
  - **Path**：
  - **Content-Type**：application/json | multipart/form-data | x-www-form-urlencoded | ...
  - **环境**：DEV / TEST / STAGING
  - **Base URL**：
- **用例类型**：正向 / 反向 / 边界 / 异常 / 权限 / 幂等 / 并发 / 限流
- **优先级**：P0 / P1 / P2
- **风险等级**：High / Medium / Low
- **是否自动化候选**：是 / 否（原因：稳定性/价值/维护成本）
- **作者 / 日期**：

---

## 2. 前置条件（Preconditions）

- **账号/角色**：普通用户 / 管理员 / 租户A / 租户B / ...
- **鉴权方式**：Bearer Token / Cookie Session / 签名 / 无
- **必要数据准备**：
  - 数据A：
  - 数据B：
- **依赖服务/开关**：

---

## 3. 请求设计（Request）

### 3.1 Headers

| Header | 取值 | 必填 | 说明 |
| --- | --- | --- | --- |
| Authorization |  | 是/否 | Bearer <token> |
| Content-Type |  | 是/否 |  |
| X-Request-Id |  | 否 |  |

### 3.2 Query Params

| 参数 | 取值 | 必填 | 约束/范围 | 说明 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

### 3.3 Path Params

| 参数 | 取值 | 必填 | 约束/范围 | 说明 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

### 3.4 Body

- **请求体（示例）**：

```json
{
  "example": "value"
}
```

- **字段覆盖点（可选）**：
  - 必填缺失：
  - 类型/格式非法：
  - 边界值（最小/最大/超界/空串/null/未传）：
  - 可选字段（不传/传空/传有效）：

---

## 4. 测试步骤（Steps）

1. 设置环境与鉴权（写清 token 获取方式或固定测试 token 来源）
2. 构造请求（按上方 headers/params/body）
3. 发送请求并记录请求与响应
4. 校验响应（状态码/业务码/字段/副作用）
5. 如涉及写入：到数据库/日志/后续接口验证状态一致性（按需）

---

## 5. 预期结果（Expected）

### 5.1 HTTP 层

- **期望 HTTP Status**：
- **响应时间要求（如有）**：

### 5.2 业务层

- **期望业务码/错误码**：
- **期望错误信息（如有）**：
- **响应体结构与关键字段校验**：
  - 字段A：
  - 字段B：

### 5.3 数据与副作用（如适用）

- **写库**：应写入/不应写入（表/字段/条数/状态）
- **状态流转**：从 X → Y（或不发生变化）
- **幂等**：重复提交仅生效一次，返回一致结果
- **越权**：不得读取/修改他人资源，不泄露敏感信息

---

## 6. 幂等 / 并发 / 限流（按需）

- **幂等键**：字段名/请求头/规则
- **重复提交策略**：同幂等键、同请求体、间隔时间
- **并发量**：N（并发请求数）
- **限流预期**：429/特定错误码/排队/降级
- **数据一致性预期**：无重复记录/无脏数据/无超卖等

---

## 7. 实际执行记录（Execution）

- **执行人 / 日期 / 环境**：
- **执行结果**：Pass / Fail / Blocked
- **实际请求（脱敏）**：
- **实际响应（脱敏）**：
- **证据**：日志链接/截图/抓包/traceId
- **缺陷记录**：BUG-xxx（严重性：S0/S1/S2/S3）
- **备注**：

